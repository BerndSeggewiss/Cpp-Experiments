namespace TheisDatensatztypRecord
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        public record Artikel(string Bezeichnung, double Preis);

        private void buttonAnzeigen_Click(object sender, EventArgs e)
        {
            Artikel a1 = new("Tisch", 54.9);
            Artikel a2 = new("Lampe", 39.9);
            labelAnzeige.Text = $"Datensatz: {a1}\n";
            labelAnzeige.Text += $"Eigenschaften: {a2.Bezeichnung}, {a2.Preis}\n";
            (string b, double p) = a2;
            labelAnzeige.Text += $"Dekonstruktion: {b}, {p}";
            Artikel a3 = new("Tisch", 54.9);
            if (a1 == a3)
            {
                labelAnzeige.Text += "\nDatensätze sind gleich";
            }
            else
            {
                labelAnzeige.Text += "\nDatensätze sind ungleich";
            }
            Artikel a4 = a2;
            labelAnzeige.Text += $"\nKopie: {a4}\n";
            Artikel a5 = a2 with { Preis = 34.5 };
            labelAnzeige.Text += $"\nGeänderte Kopie: {a5}";
            a5 = a5 with { Bezeichnung = "Stehlampe" };
            labelAnzeige.Text += $"\nGeänderter Datensatz: {a5}\n";
        }

        private void Form1_Load(object sender, EventArgs e)
        {
            labelAnzeige.Text = "";
        }
    }
}
